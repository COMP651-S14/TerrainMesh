#include "pq_walk.h"
#include "edge.cpp"
#include "predicates.cpp"
#include "vertex.cpp"

Edge * pq_walk(Vector3 q){
    //get triangulation Triangulation

    Edge e = randEdge(Triangulation);

    while(true){
        if()
    }
}

randEdge(Triangulation T){
    //get a random edge or apply some heuristic
}

leftOrOn(Edge e, Vector3 q){
    Vertex org = e.Org();
    Vertex dest = e.Dest();
    if()
}

